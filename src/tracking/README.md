# src/tracking

MediaPipe Tasks Vision integration lives here (Phases 2–3):

- `FaceLandmarker` (VIDEO mode, `outputFacialTransformationMatrixes`) → necklace + earrings.
- `HandLandmarker` (VIDEO mode) → ring + bracelet + watch.

See `BUILD_SPEC.md` §3–§4 for model assets, mapping math, and smoothing.
